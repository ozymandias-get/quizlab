/** Max tab ids kept warm (includes active); 2 => active + one background webview. */
export const MAX_ALIVE_UNPINNED_TABS = 2

/** Inactive mounted sessions sleep after this interval (webview unmounts). */
export const AI_TAB_SLEEP_MS = 3 * 60 * 1000

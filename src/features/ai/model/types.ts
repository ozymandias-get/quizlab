export interface SendTextResult {
    success: boolean;
    error?: string;
    mode?: string;
    actualUrl?: string;
}

export interface SendImageResult {
    success: boolean;
    error?: string;
    mode?: string;
    actualUrl?: string;
}

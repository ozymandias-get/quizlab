/**
 * 📐 PDF Feature — Type-only exports
 *
 * Use this barrel for type-only imports to avoid pulling in runtime code.
 */
export type { LastReadingInfo, PdfTab, ReadingProgressUpdate, ResumePdfResult } from './hooks/types'

// Reserved for future interaction mode state machine.
// Currently, interaction modes (pan, text-select, capture-area) are managed
// via individual boolean states (panMode, isInteractionBlocked, etc.)
// in PdfViewerDocument rather than a unified enum.

/**
 * PDF AI Bridge — standardizes all PDF → AI data transfer paths.
 * Centralizes metadata construction for text and image items sent to AI.
 */

export { usePdfAiActions } from './usePdfAiActions'

### Commits

8d543c6 refactor: extract inline JS from splash.html into separate file

### Stats

src/public/scripts/splash.js | 42 ++++++++++++++++++++++++++++++++++++++++++ src/public/splash.html | 44 +------------------------------------------- 2 files changed, 43 insertions(+), 43 deletions(-)

### Diff

diff --git a/src/public/scripts/splash.js b/src/public/scripts/splash.js new file mode 100644 index 0000000..d3bd620 --- /dev/null +++ b/src/public/scripts/splash.js @@ -0,0 +1,42 @@ +;(function () { + const userLanguage = navigator.language || navigator.userLanguage + document.documentElement.lang = userLanguage.startsWith('tr') ? 'tr' : 'en' + const statusElement = document.getElementById('status-text') + const subtitleElement = document.getElementById('subtitle-text') + const buildTextElement = document.getElementById('build-text') + + const copy = userLanguage.startsWith('tr') + ? { + status: '+���renme Ortam�- Ba+�lat�-l�-yor', + subtitle: 'Yapay Zeka Destekli +�al�-+�ma Alan�-' + } + : { + status: 'Bootstrapping Learning Workspace', + subtitle: 'AI-Powered Study Environment' + } + + if (statusElement) { + statusElement.textContent = copy.status + } + if (subtitleElement) { + subtitleElement.textContent = copy.subtitle + } + if (buildTextElement) { + const params = new URLSearchParams(window.location.search) + const appVersion = params.get('version') + buildTextElement.textContent = appVersion ? `v${appVersion}` : 'v0.0.0' + } + + const quitBtn = document.getElementById('quit-btn') + if (quitBtn) { + if (userLanguage.startsWith('tr')) { + quitBtn.textContent = '+�-k�-+� Yap' + } + setTimeout(() => { + quitBtn.classList.add('visible') + }, 5600) + quitBtn.addEventListener('click', () => { + window.close() + }) + } +})() diff --git a/src/public/splash.html b/src/public/splash.html index a738e6b..263fc4a 100644 --- a/src/public/splash.html +++ b/src/public/splash.html @@ -36,55 +36,13 @@ <section class="status-row"> <div class="status-dot"></div> <p id="status-text" class="status-text">Bootstrapping Learning Workspace</p> </section> <div id="build-text" class="build-text">v0.0.0</div> </main> <button id="quit-btn" class="quit-btn">Exit App</button> - <script> - const userLanguage = navigator.language || navigator.userLanguage - document.documentElement.lang = userLanguage.startsWith('tr') ? 'tr' : 'en' - const statusElement = document.getElementById('status-text') - const subtitleElement = document.getElementById('subtitle-text') - const buildTextElement = document.getElementById('build-text') - - const copy = userLanguage.startsWith('tr') - ? { - status: '+���renme Ortam�- Ba+�lat�-l�-yor', - subtitle: 'Yapay Zeka Destekli +�al�-+�ma Alan�-' - } - : { - status: 'Bootstrapping Learning Workspace', - subtitle: 'AI-Powered Study Environment' - } - - if (statusElement) { - statusElement.textContent = copy.status - } - if (subtitleElement) { - subtitleElement.textContent = copy.subtitle - } - if (buildTextElement) { - const params = new URLSearchParams(window.location.search) - const appVersion = params.get('version') - buildTextElement.textContent = appVersion ? `v${appVersion}` : 'v0.0.0' - } - - const quitBtn = document.getElementById('quit-btn') - if (quitBtn) { - if (userLanguage.startsWith('tr')) { - quitBtn.textContent = '+�-k�-+� Yap' - } - // Make clickable after animation completes (5s delay + 0.6s anim) - setTimeout(() => { - quitBtn.classList.add('visible') - }, 5600) - quitBtn.addEventListener('click', () => { - window.close() - }) - } - </script> + <script src="scripts/splash.js"></script> </body> </html>
